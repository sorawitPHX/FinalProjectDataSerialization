<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <!-- Bootstrap CDN -->
    <link href="/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        html, body {
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        body {
            margin: 0;
        }

        .container {
            flex: 1;
        }

        .footer {
            position: relative;
            bottom: 0;
            width: 100%;
        }
    </style>
</head>

<body>
    <!-- Fixed Top Navbar -->
    <nav class="navbar fixed-top navbar-expand-lg bg-dark navbar-dark shadow-sm">
        <div class="container">
            <div class="date-bar-left">
                <span class="topbar-date text-white" id="current-date"></span>
            </div>
            <a class="navbar-brand text-uppercase fw-bold" href="#">Ngamnij Arch-int</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <div class="ms-auto">
                    <ul class="navbar-nav">
                        <li class="nav-item ms-4">
                            <a class="nav-link <%= activePage === 'index' ? 'active' : '' %> text-white" href="/">หน้าหลัก</a>
                        </li>
                        <li class="nav-item ms-4">
                            <a class="nav-link <%= activePage === 'project' ? 'active' : '' %> text-white" href="/project">งานวิจัยและโครงงาน</a>
                        </li>
                        <li class="nav-item ms-4">
                            <a class="nav-link <%= activePage === 'aboutme' ? 'active' : '' %> text-white" href="/aboutme">เกี่ยวกับเรา</a>
                        </li>
                        <li class="nav-item ms-4">
                            <a class="btn btn-outline-light <%= activePage === 'login' ? 'active' : '' %>" href="#">เข้าสู่ระบบ</a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </nav>

    <!-- เนื้อหาของเว็บไซต์ -->
    <div class="container" style="margin-top: 100px;">
        <%- body %>
    </div>

    <!-- Footer -->
    <footer class="footer mt-auto py-3 bg-dark text-white">
        <div class="container text-center">
            <p>© 2024 Ngamnij Arch-int. All rights reserved.</p>
        </div>
    </footer>

    <!-- JavaScript to display current date -->
    <script>
        function formatDate(date) {
            const options = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' };
            return date.toLocaleDateString('th-TH', options);
        }

        document.getElementById('current-date').textContent = formatDate(new Date());
    </script>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="/js/bootstrap.bundle.min.js"></script>
</body>

</html>
